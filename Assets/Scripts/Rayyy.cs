using UnityEngine;
using System.Collections;

public class Rayyy : MonoBehaviour {

	// Use this for initialization
	public float Speed;
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {


//		if (Input.GetMouseButton(0)) {
//
//			Ray landray = Camera.main.ScreenPointToRay(Input.mousePosition);
//			RaycastHit hit;
//
//			if (Physics.Raycast(landray, out hit, 1000))
//			{
//				Debug.Log(hit.point);
//				//transform.position=Vector3.MoveTowards(transform.position,hit.point,Time.deltaTime*Speed);new 
//				Vector3 off = new Vector3(3f,2f,2f);
//				GetComponent<Rigidbody>().MovePosition (off	*Time.deltaTime);	
//
//			}
//
//		}
	}
}
